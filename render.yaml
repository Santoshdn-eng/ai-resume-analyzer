services:
  - type: web
    name: ai-resume-analyzer
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn --chdir backend -w 1 --threads 4 --timeout 120 -b 0.0.0.0:$PORT app:app"

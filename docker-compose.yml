version: "3.9"

services:
  backend:
    build: ./backend
    container_name: resume-analyzer-backend
    ports:
      - "5050:5050"
    volumes:
      - ./backend:/app
    environment:
      - FLASK_ENV=production
      - PORT=5050
    restart: unless-stopped

